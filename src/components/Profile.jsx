import React from 'react';
import { useAuth, useAuth } from '../context/AuthProvider';

const Profile = () => {

    const useAuth = useAuth();
    return (
        <div>
            hi
        </div>
    );
};

export default Profile;