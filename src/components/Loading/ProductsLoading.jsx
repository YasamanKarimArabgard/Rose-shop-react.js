import Skeleton from 'react-loading-skeleton'

const ProductsLoading = () => {

    return (
        {
            Array(3).map(item => {

            })
        }
    );
};

export default ProductsLoading;